#include<stdio.h>
int main()
    {
        char fname[10];
        char sname[10];

        printf("enter the first name :");
        gets(fname);

        printf("enter the sername :");
        gets(sname);
printf("%s %s",fname,sname);
       
    
    }